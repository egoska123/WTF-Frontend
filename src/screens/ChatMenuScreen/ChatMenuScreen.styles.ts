import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    paddingTop: 52,
    paddingRight: 16,
    paddingLeft: 16,
    display: 'flex',
  },
  scrollableTabs: {
    marginTop: 8,
    marginBottom: 16,
  }
});
